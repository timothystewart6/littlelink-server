import { test } from 'node:test';
import assert from 'node:assert';

test('global.d.ts has Window interface with gtag', () => {
  assert.ok(true);
});

test('global.d.ts has Window interface with umami', () => {
  assert.ok(true);
});

test('global.d.ts has Window interface with _paq', () => {
  assert.ok(true);
});
